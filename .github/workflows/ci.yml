# Runs on every push/PR: vet/build/test (linux, macOS), golangci-lint, and
# govulncheck. Separate from release.yml, which only builds and publishes
# tagged binaries.
#
# go-version is pinned to Go 1.25.13, the current patched release. Keep this
# explicit: actions/setup-go resolved the minor-only version to 1.25.12 after
# 1.25.13 shipped, leaving govulncheck to flag fixed stdlib CVEs. Advance this
# pin as supported 1.25 patches are released.
name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: '1.25.13'

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test
        run: go test ./...

      - name: Gofmt check
        run: |
          fmt_out="$(gofmt -l .)"
          if [[ -n "$fmt_out" ]]; then
            echo "The following files are not gofmt-formatted:"
            echo "$fmt_out"
            exit 1
          fi

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: '1.25.13'

      - uses: golangci/golangci-lint-action@v9
        with:
          version: latest

  vulncheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: '1.25.13'

      - name: govulncheck
        run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...
